# API

API documentation will be added here (endpoints, payloads, auth).

Planned endpoints:
- `POST /auth/login/`
- `GET /fields/`
- `POST /fields/`
- `GET /fields/:id/`
- `POST /fields/:id/updates/`
- `GET /dashboard/`
